# 2026-03-27

## Lessons Learned
- **Always journal after every work session** — failed to log Mar 25-26 work, lost context. Hao had to remind me about `.venv` and project details.
- `.venv` exists at `/Users/lhooz/.openclaw/workspace/.venv` with jax/numpy installed. Use `source .venv/bin/activate` before running python.
- Semantic memory_search was down (embedding provider error). Fallback: read memory files directly.

## SNN 2D Navigation Project Timeline (reconstructed)
- **Mar 25:** 1D event camera SNN (`snn_event_camera.py`) developed and trained
- **Mar 26:** 1D SNN params saved (`snn_params.npz`), training curve generated
- **Mar 27 ~00:11:** 2D navigation scene + event camera (`event_camera_2d_nav.py`)
- **Mar 27 ~00:34:** 2D SNN trained with old linear readout (`W_out` + `b_out`), params saved as `snn_2d_params.npz`
- **Mar 27 ~13:15:** Surgery applied — `snn_2d_nav.py` rewritten with LI (Leaky Integrator) readout replacing flat linear readout
  - New params: `(W1, W2, W_li)` — no `W_out`, `b_out`
  - LI dynamics: `U_t = β_li * U_{t-1} + S_t @ W_li`, β_li=0.92
  - Output: `tanh(U_T)` at last timestep
  - **NOT YET TRAINED** with new architecture

## Current State
- `snn_2d_params.npz` contains OLD params (pre-surgery): W1, W2, W_out, b_out
- New code in `snn_2d_nav.py` expects W_li — needs fresh training run
- Environment ready in `.venv`
